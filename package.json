{
  "name": "ci-dashboard-visualization",
  "version": "1.0.0",
  "description": "A visualization tool for CI/CD workflows",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.0.0"
  }
}